[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% set breaking = commits | filter(attribute="breaking", value=true) %}\
{% if breaking | length > 0 %}
### ⚠️ Breaking Changes
{%- for commit in breaking %}
- {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{{ commit.message | upper_first }}
{%- endfor %}
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{%- for commit in commits %}
- {% if commit.scope %}**{{ commit.scope }}:** {% endif %}{{ commit.message | upper_first }}{% if commit.body %}: {{ commit.body | trim | replace(from="\n", to=" ") }}{% endif %}
{%- endfor %}
{% endfor %}
"""
trim = true
footer = ""

[git]
conventional_commits = true
filter_unconventional = true
commit_parsers = [
  { message = "^feat", group = "Added" },
  { body = "(?i)\\bRUSTSEC-|\\bCVE-|\\bsecurity vulnerability\\b|\\bsecurity advisory\\b|\\bsecurity issue\\b", group = "Security" },
  { message = "^fix", group = "Fixed" },
  { message = "^refactor", group = "Changed" },
  { message = "^perf", group = "Changed" },
  { message = "^chore\\(release\\):", skip = true },
  { message = "^chore\\(deps.*\\)", skip = true },
  { message = "^chore", skip = true },
  { message = "^.*: remove", group = "Removed" },
  { message = "^.*: deprecate", group = "Deprecated" },
  { message = "^doc", group = "Changed" },
  { message = "^style", group = "Changed" },
  { message = "^test", skip = true },
  { message = "^ci", skip = true },
  { message = "^audit", skip = true },
  { message = ".*", skip = true },
]
sort_commits = "oldest"
protect_breaking_commits = true

[bump]
breaking_always_bump_major = false
